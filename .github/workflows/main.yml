name: CI

on:
  pull_request:
    branches:
      - master
      - release/v[0-9]+.[0-9]+.[0-9]+

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Run tests
        run: |
          make test
